# Journey-1 saved/learned identity

This note records a sanitised source reproduction for LQ-1. The exact 23 September client catalogue and deployed production SHA are unavailable; the synthetic coordinates are not a historical trace.

Fresh `origin/main` for the implementation base was `d641e5daf5402087d280cc6de19adca57b91691c`. Before editing, the pure engine produced one commute across the intermediate Home visit with an overlapping accepted learned candidate. The same occurrence evidence with a saved-only catalogue produced a separate intermediate Home stay and commute legs. Receipt times were later than occurrence times in both runs.

The regression fixture preserves the two Home episodes, a brief station drop-off that does not become a stay, and distinct final arrival. PR #208 fixes the intermediate Home stay being absorbed by the saved/learned identity conflict. Its first head made the weak `return-route-3` match ambiguous and delayed the synthetic final Home boundary from 19:49:00Z to 19:51:48Z. The correction resolves that approach observation to saved Home only after two later strong saved observations support it within existing timing windows; the corrected synthetic final boundary and both Home stay identities match the saved-only baseline.

The special match path applies only with exactly one plausible saved and one accepted learned candidate at near-coincident centres. Two high-quality strong matches keep saved identity when the matcher receives no learned active-place input and saved computed priority is at least learned priority; otherwise its existing deterministic order applies. The direct matcher accepts active-place input, but this replay path does not supply it. An isolated low-accuracy Visit or plausible overlap remains ambiguous; the broad-accuracy Visit in this fixture gains saved identity only from independent high-quality observations near both ends. Distinct nearby candidates and a learned-only catalogue retain their deterministic matches. Server replay and the shared engine use the same fixture output. Replay tests also cover protected changed-ID replacements and same-ID manual/terminal decisions.

This evidence does not establish or repair the historical approximately 18:29 Home return or the long post-drop-off gap: authoritative retained evidence for that earlier return is unavailable. It also does not establish the historical on-device catalogue or validate staging, production, physical capture, or previously decided records in a live database. School-1, POI naming, duration formatting, and missing-return capture remain separate.
