# Journey-1 saved/learned identity

This note records a sanitised source reproduction for LQ-1. The exact 23 September client catalogue and deployed production SHA are unavailable; the synthetic coordinates are not a historical trace.

Fresh `origin/main` for the implementation base was `d641e5daf5402087d280cc6de19adca57b91691c`. Before editing, the pure engine produced one commute across the intermediate Home visit with an overlapping accepted learned candidate. The same occurrence evidence with a saved-only catalogue produced a separate intermediate Home stay and commute legs. Receipt times were later than occurrence times in both runs.

The regression fixture preserves the two Home episodes, a brief station drop-off that does not become a stay, and distinct final arrival. Matching keeps a saved identity only for a near-coincident saved/learned pair with two strong matches and no stronger learned continuity or correction priority. The broad-accuracy Visit starts ambiguous and gains the saved identity only from separate high-quality observations near its start and end, without contradictory evidence. A plausible pair remains ambiguous; a distinct nearby learned place keeps its own identity. Server replay and the shared engine use the same fixture output. Replay tests also cover protected changed-ID replacements and same-ID manual/terminal decisions.

This evidence does not establish the historical on-device catalogue or validate staging, production, physical capture, or previously decided records in a live database.
