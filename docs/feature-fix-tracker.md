# Dayframe Feature And Fix Tracker

This is the canonical delivery-state snapshot. Product intent belongs in `docs/PRD.md`, architecture in `docs/architecture.md`, validation rules in `.codex/reference/validation-matrix.md`, and detailed implementation evidence in `docs/investigations/` and Git history.

Last repository audit: 2026-09-08 against `origin/main` at merge `ae60f6d` (PR #191).

## Evidence snapshot

- Main includes PRs #149–#191. PRs #186–#191 add the canonical five-minute automatic policy and durable structural Review actions, Review/Location contention repair, bounded server transactions and receipt reconciliation, independent mobile recovery plus deliberate Settings sync, a durable Health source journal, authoritative Location V2 replay, exact Visit departure boundaries, and the isolated staging iOS identity.
- The latest verified internal TestFlight release is `0.1.0 (100)`, delivery/build ID `10f84f0c-de70-4c77-b54c-1f7d4b7d8125`, archived from merge `6c4a168` and recorded as `VALID`/`IN_BETA_TESTING` on 2026-08-29. It includes main through PR #185, uses the production API, has export compliance set to false and en-GB notes, and is available through `Internal Health Debug`; external testing remains disabled.
- PR #182's exact staging head was signed, installed, launched, and owner-tested on the attached iPhone 11 before merge. The real two-connection local PostgreSQL advisory-lock contention test was rerun after merge and passed before build 97 was released.
- PR #169's Preview was promoted to staging and exercised through a signed preview build on the attached iPhone. This tracker does not infer production deployment or TestFlight status from that staging evidence.
- Preview uses staging Supabase and the manually promoted `dayframe-staging.vercel.app` alias. Production/TestFlight use `dayframe-web.vercel.app` with production Supabase. Preview and production now have separate iOS bundle, App Group, Keychain, URL-scheme, Live Activity extension, and on-device identities.
- PR #183 is merged at `2b732d2` with repository coverage for cache-first Review/Location Evidence, local terminal-action acknowledgement, cancellation and bounded server work. It is included in TestFlight build 100, but no exact PR #183 Preview, signed staging build, or physical-iPhone result is recorded here.
- PR #184 is merged at `6fd55b83` with repository coverage for one root connectivity owner, durable-work projection, ordered reconnect/retry, account isolation, fixed header presentation, and timer Stop/Edit/Delete diagnostics. It is included in TestFlight build 100; repository/simulator evidence does not substitute for the physical network/account matrix.
- PRs #188–#191 are merged at `2dc2a90`, `108bfc0`, `3f91d08`, and `ae60f6d`. GitHub reports their documentation, disposable PostGIS, and clean iOS Simulator checks successful. The exact PR #190 combined head `ce9f5a0` was promoted to stable staging, and a pre-final PR #191 head was recorded there later; no exact final PR #191 promotion, production deployment/migration, TestFlight release, or signed-device acceptance is recorded.

## Status key

- `Done`: present on main with appropriate repository-level evidence.
- `Watch`: merged, but real data, production, or physical-device behavior still needs observation.
- `Release pending`: present on main but not in the last repository-recorded relevant runtime, such as the verified mobile binary or production deployment.
- `Implementation pending`: present only on the active implementation branch and still requires merge plus release evidence.
- `Decision needed`: implementation should not advance until the owner chooses the product/operational policy.
- `Future`: explicitly out of the current implementation lane.

## Current delivery state

| Area | Status | Current evidence and boundary |
| --- | --- | --- |
| Event-first manual timer and completed-entry writes | Done | Web/mobile start, stop, switch, split, and completed manual creation flow through `activity_events` before derived entries; focused service/route coverage exists. Existing entry edits/deletes mutate derived data rather than fabricating new capture signals. |
| Category/task-first UX and reusable tags | Done | Description and category remain optional; projects/clients are compatibility-only. Tags have workspace-scoped storage, web/mobile editing, offline queued-start support, shared native Calendar presentation, and a 30-colour category palette contract. Eligible web timer, Add Time, Timeline List, and Calendar create/edit pickers can also create an unpinned category without leaving or submitting the current draft; the deterministic automatic colour remains the default and can be replaced before creation from the shared palette. |
| Persistent web timer and Timeline | Done | One shell timer owner spans Dashboard/Timeline. Calendar/List/Timesheet, click-to-create, eligible pointer resize, shared quick editing, grouped description/time editing, overlap handling, and Today rollover are on main through PR #165. |
| Web reports, search, settings, place management, and export | Done | Range-scoped reports, historical search, shared entry editor, hosted place search adapter, workspace settings, CSV/JSON export, and responsive/focus guardrails are implemented. Saved places and one-time location labels share the public `placeName` read path; reports group normalized one-time labels. Provider-backed web place search still depends on a configured server key. |
| Mobile Reports Stage A | PR #194 open — V3.2 refinements, acceptance pending | V3.2 overrides conflicting V3.1/V3 presentation rules while retaining their unaffected safety requirements. The branch uses one daily Today/custom-day bucket; period-aware fitted labels/bars; exact-duration sorted donut/list; a zero-gap compact summary; the shared 349-point calendar-content cap; equal compact presets; and shared handle-only swipe-dismiss ownership for both Reports sheets. Cancel remains and there is no Clear/reset-to-Today path. Synthetic iOS 26.5 layout evidence at the current working tree measures category pitch from 56 to 38 points and keeps complete numbers at MAX text; this is not a signed final-head or physical-device acceptance result. Accounting/API/cache/timer/Review/Health/Location/sync owners remain unchanged. Bold Text, authenticated staging, physical motion/accessibility/timer checks and new Claude whole-PR review remain open; evidence is in `docs/investigations/2026-09-10-reports-final-fixes.md`. No merge, production testing or Stages B–D. |
| Mobile shared time-entry sheets and tag persistence | Watch | PRs #163 and #164 are included in TestFlight build 93. Repository tests cover keyboard/tag/layout/persistence paths; continue physical-iPhone lifecycle, keyboard, tag, and sheet-exit observation. |
| Native iOS Calendar | Watch | SwiftUI/UIKit Calendar, one native pinch/scroll owner, semantic blocks, long-press creation, React-owned sheets/mutations, dedicated saved/one-time location text, and contract tests are implemented. Continue physical-iPhone gesture, callback, accessibility, and frame-pacing checks. |
| Offline event capture and timer fallback | Watch | General event queue has retry/backoff, foreground drain, idempotency, and diagnostics. PR #176's lifecycle/Keychain recovery, PR #179's bearer ownership hardening, and PR #182's dedicated Stop outbox are included in TestFlight build 97; continue real-device reconnect, suspended/background, ordered dependency, and cross-device reconciliation observation. |
| Mobile explicit Stop durability and contention ([Trello #147](https://trello.com/c/XBVc6pe6/147-mobile-timer-stop-hangs-during-background-event-sync)) | Watch | PR #182 adds an account-owned storage-only Stop outbox, persist-before-dismiss acceptance, malformed-container recovery, authenticated generation/token revalidation before dispatch, exact `/api/events` replay, bounded mobile delivery, entry-scoped server locking and fail-closed legacy handling. Focused storage/session/route/idempotency coverage and the real two-connection PostgreSQL contention test pass. The exact staging head was signed and owner-tested on the attached iPhone 11, and the merge is in TestFlight build 97; continue production offline/force-quit/account-switch observation. |
| Review automation and durable complex corrections | Release pending | PR #186 is merged at `5fd50cf`. The five-minute confidence/boundary/overlap policy and durable structural actions are on main. Historic branch and staging limitations remain evidence gaps, not unmerged implementation state; the behavior is newer than verified TestFlight build 100. |
| Offline Review resolving and structural actions | Release pending | PR #183 supplied cache-first SQLite v4 and three durable terminal actions in build 100. PR #186 advances the store through v5 effects so all resolving/structural Location actions—including save-place, record-once, POI, split, and merge variants—commit locally before dismissal. PRs #188–#189 add v6 interruption/receipt reconciliation, acknowledgement validation, independent due times, and explicit manual follow-up coalescing. Canonical-open conflicts restore per source at stored anchors; the bounded Location Evidence cache remains seven days/25 items/5 MiB. These later contracts are on main but not in a verified newer binary. |
| Global mobile connectivity status and ordered reconnect | Watch | PR #184 is on main at `6fd55b83`. One root owner presents the fixed 44-point status slot, account-owned durable projection, ordered reconnect/retry, negative HTTP evidence, permanent timer Stop/Edit/Delete diagnostics, and Retry/Discard controls. General Starts, explicit Stops, offline Edit/Delete, Review, Location, and native hand-off work feed one account coordinator; repeated positive reachability cannot erase stronger current evidence or reset bounded retry. It is included in TestFlight build 100, but repository/simulator tests do not substitute for a fresh physical-iPhone network, account, and timer convergence matrix. |
| Timer background completion, mutation responsiveness, Desk convergence, and mobile card alignment | Watch | PR #185 is merged at `6c4a168` and included in TestFlight build 100. It adds one finite shared iOS assertion for actively transmitting durable Start/Switch/Stop/Edit/Delete work, static pending versus rotating transmission status, APNs-independent authoritative mutation/fingerprint responses, exact-entry idempotent web Stop plus latest-intent sequencing, and corrected shared idle/running card geometry: 136-point baseline height, fixed top controls, Plus/visible-pill bottoms at 122 points, equal 14-point visible outer padding, and 44-point effective pill targets independent of their 32-point bodies. Stable staging and signed iPhone 11 evidence exist for the exact PR head; continue the physical offline/background/account-isolation and visual acceptance matrices against build 100. |
| Mobile login and Review sync follow-up | Watch | PR #180 adds bounded auth/bootstrap/queued-event/Review deadlines, an atomic branded post-auth opening state, retry-action semantics, and the time-only automatic Location Evidence editor with live Duration. It is included in TestFlight builds 96 and 97; continue physical-iPhone slow/stalled-network observation. |
| Mobile Review and Location Evidence presentation | Watch | Review uses separate activity cards with inset straight category rails, prominent activity/time, accessible confidence, and a concise reason automatic logging did not apply. Commutes show only honest route evidence plus What/When. Unknown stays load up to three nearby Apple POIs, use a bounded transient contextual lookup and destination diversity when Apple exposes a repeated site name, retain typed search/map fallback, and default to one-time recording with an opt-in saved place; generated unknown activity starts empty, category visuals stay compact inside 44 pt targets, and the outer evidence scroll owner keeps focused Search/Activity fields above the iOS keyboard. PR #172's exact Preview from `f0904b1` was promoted to staging and its signed Staging build was installed on the attached iPhone. The owner reports successful PR #174 testing on the attached iPhone 11, and PRs #173–#175 are included in TestFlight build 93; continue production-data and lifecycle observation. |
| Health capture reliability | Release pending | PR #190 is merged at `3f91d08`. The backend/workspace/user-owned journal atomically preserves query changes, tombstones, complete split-query episodes, checkpoints, immutable payloads/IDs, queue ownership, and exact acknowledgement; server source decisions preserve prior ignore/entry outcomes and link later Sleep revisions through `activity_events.resolved_time_entry_id`. The additive migration was recorded on staging before merge, but production migration/deployment, a verified newer TestFlight build, physical HealthKit evidence, and legacy environment provenance remain release gates. Additional repair UI and wider legacy-environment migration remain deferred. |
| HealthKit sleep/workout import | Watch | Event-first import, grouped sleep sessions, mapping defaults, reprocess diagnostics, same-source untouched-entry reconciliation, and migration `202608010001_health_sleep_session_reconciliation.sql` are on main. Background delivery and real HealthKit sample revisions remain physical-device/production concerns. |
| Mobile Review/Location recovery reliability | Release pending | PR #187 is merged at `d649e2d`; PRs #188–#189 add bounded reconciliation, retained request identity, full JSON deadlines, independent lane progress, and partial Location outcomes. The production alias was verified serving PR #187 on 2026-09-04. Installed build 103 was observed as valid but its source/API/runtime chain was not attested, so it is not release evidence for current main. The originally reported records remain unresolved; see [sync recovery evidence](investigations/2026-09-04-sync-recovery-execution.md). |
| Bounded sync and receipt reconciliation | Release pending | PR #188 is merged at `2dc2a90`. Review mutation IDs/hashes and atomic receipts remain authoritative; interrupted/contended mobile work first uses the bounded read-only reconciliation route, and Health/Location/Review operations use checked-out clients with finite transaction, statement, lock, and cleanup budgets. The resolution-link migration is in source and recorded on staging, but production migration/deployment and incident repair are not recorded. |
| Mobile bounded recovery and deliberate sync | Release pending | PR #189 is merged at `108bfc0`. Full JSON deadlines, acknowledgement validation, Review reconciliation, lane-specific due times, partial Location outcomes, session/account guards, and one retained follow-up for repeated Settings Sync are on main. The 45-second manual coordinator lets Health, activity, Review, Location, and final refresh settle independently; a busy lane cannot gate the others. Signed-device and physical recovery acceptance remain outstanding. |
| Location V2 authority and Visit departure boundaries | Release pending | PR #191 is merged at `ae60f6d` after #188 → #189 → #190. V1 semantics are suppressed in every V2 mode, including silent `v2_shadow`; evidence upload acknowledgement and bounded semantic replay are separate. The shared engine preserves a valid completed Visit departure as the exact stay stop/commute start while contradictory-place or later-inside evidence falls back to bounded estimation. The staging App Group mismatch is repaired through entitlement-aligned, identity-checked configuration. Exact-final staging promotion, production rollout/cutover, signed-device isolation, and physical acceptance remain release gates. See [four-PR review](investigations/2026-09-07-four-pr-stack-review.md) and [App Group repair](investigations/2026-09-08-pr191-app-group-isolation.md). |
| Location Intelligence V2 foundation | Watch | Deterministic shared engine, protected mobile journal, separate upload/replay transactions, server evidence/segments, Review DTO, durable split/merge/place/record actions, retention cron, and the narrow automatic policy are implemented. Strong trusted stays and standard saved-endpoint commutes use the normal lane; the documented medium commute exception remains guarded by distinct verified endpoints and stronger route evidence. Retention/logout clearing cannot reconstruct already-removed evidence. Checked-in fallback remains silent `v2_shadow`; live rollout is the operational decision below. |
| Live Activity/App Intent sync ([Trello #148](https://trello.com/c/UN0A4CbV/148-duplicate-optimistic-and-canonical-live-activities-on-iphone)) | Watch | PR #160's App Group/Keychain hand-off, direct Stop fallback, APNs registration, revisioned delivery outbox, diagnostics, and retry cron remain the baseline. PR #182 adds awaited snapshot-verified native cleanup, zero-or-one canonical Activity per generation, registration/Stop revalidation, and stale same-entry registration invalidation. Automated coverage passes, the exact staging head was signed and owner-tested on the attached iPhone 11, and the merge is included in TestFlight build 97; continue locked/background/terminated replacement observation. |
| Hosted provider auth and staging lane | Release pending | Supabase identity plus Dayframe sessions, workspace/user scoping, stable staging alias process, preview/production EAS targets, and staging badge are implemented. PR #191 adds `com.layereight.dayframe.staging` plus isolated App Group, Keychain, URL scheme, Live Activity extension, and on-device state, with fail-closed native group resolution and unsigned build/configuration checks. Exact signed-device host/extension entitlements and cross-process hand-off still need validation before the staging identity is treated as release evidence. |
| Workspace/time/event/review export | Done | JSON/CSV APIs and local workspace export exist; retained Location V2 evidence is included where documented. Restore/import confidence and sole-system-of-record readiness remain future work. |
| Account/workspace deletion and backup-retention policy | Decision needed | Entry/evidence deletion paths exist, but full account/workspace deletion, integration-token cleanup, raw Health/location deletion, backup/log retention, and user-facing confirmation semantics are not complete. |
| Automation accuracy metrics | Decision needed | Review outcomes are stored, but the PRD's former claim that anonymized accepted/ignored accuracy analytics were shipped is not supported by a dedicated product surface or analytics implementation. Decide whether this is an internal report, privacy-preserving telemetry, or out of scope. |

## Decision register

| Decision | Why it cannot be inferred safely | Options to decide between |
| --- | --- | --- |
| Production Location V2 mode | Code supports `v1`, `v2_shadow`, `v2_review`, and narrow `v2_enabled`; repository defaults intentionally fail closed. | Keep shadow, enable Review-only, or operationally enable the documented trusted-stay and standard/medium saved-route policy after staging/device evidence. |
| Automation accuracy measurement | Stored Review outcomes could support a local report or external analytics, with materially different privacy implications. | Remove from MVP, add an owner-only in-product report, or design privacy-reviewed telemetry. |
| Full deletion and retention | Hard deletion interacts with derived entries, exports, server logs, backups, integration tokens, and legal/user expectations. | Define immediate hard delete, staged retention, or anonymized remnants and document backup behavior. |
| Native NFC beyond Apple Shortcuts | The shipped path uses App Intents/Shortcuts; a native scanner adds entitlement, UX, and background constraints. | Keep Shortcuts-only or approve a separately scoped native NFC design. |
| Wider beta/App Store lane | MVP remains personal/friends internal TestFlight. | Continue internal-only or define external TestFlight/App Store readiness criteria. |
| Persisted activity/category icons | A display-only mobile glyph can be derived safely, but stored custom icons affect web, React Native, native Calendar, export, accessibility, and legacy clients. | Keep derived presentation glyphs, approve a shared first-party icon enum, or design user-uploaded/custom icon metadata separately. |
| Commute endpoint correction | Current commute evidence renders a route, while saved-place correction and bounded match feedback are stay-owned. Editing origin/destination would change segment and learning semantics. | Keep commute route read-only or define atomic origin/destination correction and feedback rules. |

## Future tracks

- Safe restore/import and larger-data reporting confidence.
- Token-management UI and deliberately scoped Home Assistant/local bridge inputs.
- Calendar integration hints, realtime transport, and richer automation-rule management.
- Voice/diary correction intake through a review-first audit trail.
- Billing, teams, and non-iOS mobile remain out of scope unless the PRD changes.

## Maintenance rules

- Update this file in the same PR when a shipped/watch/release/decision state changes.
- Verify GitHub state before calling work `In progress`; dated investigation notes are not current-state evidence.
- Record exact merged PR/commit, deployment, migration, TestFlight build, or physical-device evidence only when it was actually checked.
- Keep detailed validation logs in the relevant investigation or PR, not in this tracker.
- Run `npm run check:docs` and the pre-PR documentation checklist before publication.
