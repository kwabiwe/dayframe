# Dayframe Feature And Fix Tracker

Last verified: 2026-07-24 (PR #102 merged into `main` at `a7e8ba8`; focused Calendar readability/restart work is active)

## Verification Snapshot

- Local repo: `origin/main` contains merged PR #102 at `a7e8ba81a638ca4aa68d8a6c27dde5a2aabc56e3`.
- GitHub baseline: PR #85 is merged as `2c3e151` and is available in TestFlight for physical-iPhone verification.
- Latest verified TestFlight build: `0.1.0 (63)`, delivery/build ID `f0f3683b-c37c-4b2e-b457-906f129712cf`, `VALID` and `IN_BETA_TESTING` through `Internal Health Debug`; export compliance is false and en-GB notes are set.
- Vercel production is Ready with the Tags routes live; `/api/tags` returns the expected unauthenticated `401`. Hosted Supabase has the Tags columns, composite workspace constraints, RLS, and association policies. The mobile archive uses API base `https://dayframe-web.vercel.app`.
- Web app parity audit completed for recent mobile-heavy PRs #32-#57; suitable web follow-ups are tracked below and mirrored to Trello with the `Web App` label. Native-only work such as App Intents, Live Activities, background HealthKit observers, iOS Settings navigation, and keyboard/safe-area fixes is intentionally excluded from the web backlog.
- Evidence checked: recent memory, previous chat/session logs, local git log, GitHub PR/issues state, project docs, README, local archive/export/upload logs, and App Store Connect build state. Vercel deployment and production Supabase schema are checked when the shipped change affects web/API/database behavior.

## Status Key

- `Done`: merged to `main`; TestFlight/build evidence is listed separately when applicable.
- `Watch`: merged, but keep watching real production/TestFlight behaviour because the bug depended on real data or device state.
- `Release pending`: merged to `main`, but not yet verified in a new TestFlight build.
- `In progress`: implemented on an active branch, with review or merge still pending.
- `Next`: intended for the next implementation PR.
- `Planned`: accepted backlog item, no active branch yet.
- `Future`: larger track, useful but not immediate.

## Current Next Work

| Item | Status | Evidence | Next action |
| --- | --- | --- | --- |
| Web Calendar readability and restart actions | In progress | Branch `codex/web-calendar-readability-restart` starts from merged PR #102 at `a7e8ba8`. It makes density rendered-height/title-first, lane-splits minimum-height collisions, restores normal-opacity Running text, gives every block one semantic primary action and viewport-safe details, limits direct resize to safe heights, and routes Calendar/List restart through the single shell timer owner. Full validation passes with 741 tests. Optimized-production checks cover all three zooms, 1440/1280/1024/768/390 widths in System/Light/Dark plus a 720x450 200%-zoom equivalent, delayed duplicate activation, offline rollback, active conflict, keyboard, touch-width actions, Edit and resize. Investigation: `docs/investigations/2026-07-24-web-calendar-readability-restart.md`. | Review the focused draft PR after it is linked. Keep grouped List, Reports popovers, Search, mobile, migrations, deployment and merge out of scope. |
| Web Timeline range and toolbar unification | Done | PR [#102](https://github.com/kwabiwe/dayframe/pull/102) merged into `main` at `a7e8ba81a638ca4aa68d8a6c27dde5a2aabc56e3`. It replaces the shell/route/local Timeline date owners with canonical URL `date` / `scope` / `view`, one route toolbar, clipped overlap semantics across Calendar/List/Timesheet and totals, weekly Timesheet normalization, scope-aware Alt navigation, cached one-request period reads, and calm slow/offline retention. Full validation passed with 732 tests. Optimized-production checks covered 1440/1280/1024/768/390 widths in System/Light/Dark plus a 720 px 200%-zoom equivalent without page overflow; request tracing measured zero duplicate initial bootstrap calls, zero view/scope calls, and one API call for an uncached range. Investigation: `docs/investigations/2026-07-23-web-timeline-range-toolbar.md`. | Keep one URL-owned period model, one toolbar, overlap clipping, Timesheet normalization, request bounds, and slow/offline retention in regression coverage. |
| Web persistent timer-strip refinement | Done | PR [#101](https://github.com/kwabiwe/dayframe/pull/101) merged into `main` at `c850c9664bd92aad84207bdd27facc3d5b0ae140`. It implements the supplied five-part timer composition with a flexible task compound field, adjacent Category, equal 44 px Plus/Play/Stop footprints, stable elapsed time, compact 840 px reflow, phone-safe label truncation, consolidated timer CSS, and preserved single-shell mutation ownership. Two optimized-build visual passes covered 1440/1280/1024/768/390 widths across System/Light/Dark with no horizontal overflow. Full validation passed with 693 tests. Investigation: `docs/investigations/2026-07-23-web-timer-strip-refinement.md`. | Keep the single-shell timer owner, measured five-part geometry, mobile reflow, truncation, touch-target and no-overflow contracts in regression coverage. |
| Web focus, spacing and control consistency | Done | PR [#100](https://github.com/kwabiwe/dayframe/pull/100) merged into `main` at `be97f1fc7aff0e681525556dcc2119119980826f`. It gives field-like controls one in-perimeter focus owner, gives compound fields one `focus-within` owner, keeps standalone action focus distinct from selected/error/disabled states, consolidates existing web control/spacing tokens, and makes the Search shortcut label platform-neutral without changing Ctrl/Command behaviour. Two optimized-build visual passes completed the desktop/390 px route matrix across light/dark themes with no page overflow or runtime overlay. Full validation passed with 690 tests. Investigation: `docs/investigations/2026-07-23-web-focus-spacing-foundation.md`. | Keep the shared focus-owner, reserved-perimeter, spacing-token, platform-shortcut, overflow and mobile-overlay contracts in regression coverage. |
| Web authentication/session stability | Done | PR [#99](https://github.com/kwabiwe/dayframe/pull/99) merged into `main` at `85e513a49524238a73cee965c3e191d76112fadb`. Current-main production reproduction proved that prefetching the state-changing `GET /logout` revoked sessions without a click and that the uncontrolled form-action flow caused the cleared-login flash. The merged fix makes logout explicit POST-only, adds typed safe session reasons, validates the fixed TTL, reduces bootstrap reconciliation from one second to 30 seconds/focus/visibility/mutations, and keeps one continuous branded login transition. No migration or breaking mobile contract. Investigation: `docs/investigations/2026-07-23-web-auth-session-stability.md`. | Keep the documented provider-auth, 10-minute, Safari/WebKit, hostname, two-tab, expiry/revocation, safe-log and host-scoping cases in regression coverage. |
| Web Settings and final consistency sweep | Done | Phase 6 merged through PR [#98](https://github.com/kwabiwe/dayframe/pull/98) at `76f49c79e30cba92aaafb5c1b84787c7cfce94cd`. It consolidates consumer Settings, moves persistent account/workspace editing out of the quick profile surface, uses shared Appearance and SettingsRow primitives, keeps permission status factual, adds calm route loading/error states, and preserves existing API/mobile/schema contracts. Investigation: `docs/investigations/2026-07-23-web-settings-polish.md`. | Keep the merged Settings ownership, mobile overlay, provider-auth, session-expiry, export, and disposable-data evidence-deletion contracts in regression coverage. |
| Web place-search parity | Done | Phase 5 merged through PR [#97](https://github.com/kwabiwe/dayframe/pull/97) at `a07e2d204bc6aecc12138f3d2a2901a4db0d74c6`. It adds an authenticated server-only Geoapify adapter, normalized no-store search API, accessible stale-safe autocomplete, route-backed create/edit/learned-place flows, map/fallback preview, concise place lists, and focused privacy/compatibility coverage without a migration. Investigation: `docs/investigations/2026-07-23-web-place-search.md`. | Configure and verify `GEOAPIFY_API_KEY` separately in an authorised hosted Preview before treating provider-backed autocomplete as hosted evidence. |
| Web Reports overhaul | Done | Phase 4 merged through PR [#96](https://github.com/kwabiwe/dayframe/pull/96) at `9b5323f94d62db190f2fbbad8c1916391ab20c1b`. It adds a canonical URL filter model, shared clipped-entry SQL, current/previous summaries, daily/category/tag/place/source analysis, bounded editable detail, filtered CSV, focused security/calculation tests and the full browser matrix without a migration. Investigation: `docs/investigations/2026-07-22-web-reports-overhaul.md`. | Keep URL reconstruction, personal isolation, clipped durations, CSV parity and bounded-detail contracts in regression coverage. |
| Web Dashboard intelligence | Done | Phase 3 merged through PR [#95](https://github.com/kwabiwe/dayframe/pull/95) at `d30bb1cbb517a1f2cc8873749a41e472e0ed485c`. It adds clipped day/week category allocation, goal and previous-period progress, top category, compact review/activity and focused calculation tests while preserving the shell timer owner. Investigation: `docs/investigations/2026-07-22-web-dashboard-intelligence.md`. | Keep clipped overlap, running-entry, allocation, report-link and shell-timer ownership contracts in regression coverage. |
| Web persistent timer, shell and navigation | Done | Phase 2 merged through PR [#94](https://github.com/kwabiwe/dayframe/pull/94) at `32f9e4f4f48850332d02bb624e4596124a52f79d`. One AppShell runtime owns the Dashboard/Timeline timer, optimistic mutations and manual-entry presentation; redundant top chrome and independent page timer owners are removed. Investigation: `docs/investigations/2026-07-22-web-persistent-timer-shell.md`. | Keep timer singularity, refresh, optimistic rollback, date context and mobile shell behaviour in web regression coverage. |
| Web UI foundations | Done | Phase 1 merged through PR [#93](https://github.com/kwabiwe/dayframe/pull/93) at `51c16348b50a8daea00fbfb2bde2717e96a58d50`. It established shared buttons, fields, segmented controls, dialogs and popovers, consolidated focus styling, and completed the desktop/mobile browser matrix. Investigation: `docs/investigations/2026-07-22-web-ui-foundations.md`. | Keep the shared primitive, focus, modal ownership and phone-overflow contracts in web regression coverage. |
| Web authentication and personal Reports isolation | Done | Phase 0 merged through PR [#92](https://github.com/kwabiwe/dayframe/pull/92) at `f036c5c0c9775db923e4b4a98dfa719a7fc5a685`. Current-main reproduction had shown transient database errors on the public landing page and cross-user report aggregation within one workspace; the merged change added request-cached optional session handling, narrow anonymous-error handling, bounded session timestamp writes, one client `401` path and user-scoped Reports queries. Investigation: `docs/investigations/2026-07-22-web-auth-report-safety.md`. | Keep hosted provider login/expiry/logout and same-workspace two-user Reports isolation in regression coverage. |
| Live Activity start race and running-entry start-time keyboard avoidance | Watch | PR #91 merged as `84a0bfc` and shipped in TestFlight build `0.1.0 (63)`, delivery/build ID `f0f3683b-c37c-4b2e-b457-906f129712cf`; App Store Connect is `VALID`, export compliance is false, en-GB notes are set, and the build is `IN_BETA_TESTING` through the all-build `Internal Health Debug` group. Live Activity reconciliation is serialized with latest-state-wins semantics, and focusing the start time reveals the complete control group above the keyboard. Investigation: `docs/investigations/2026-07-22-live-activity-and-edit-sheet-keyboard-regressions.md`. | On a physical iPhone, start a timer and verify the Dynamic Island and Lock Screen activity remain present through optimistic-to-persisted reconciliation; then focus the running-entry start time and verify the complete control stays above the keyboard, including Reduce Motion. |
| Location Intelligence V2 trusted-place automation | In progress | PR #86 shipped the V2 capture/replay foundation in build 59 and PR #88 is live in build 61. The active branch implements the owner-approved `v2_enabled` policy: only completed `medium_high`/`high` stays matched to logging-enabled saved or accepted-and-linked learned places auto-create event-first confirmed entries; commutes, unknown/ambiguous matches, lower confidence, uncertain gaps, missing place linkage, and overlaps remain in Review. A fresh disposable PostGIS validator covers automatic writes, overlap fallback, retries, and no dual entry/review output. Investigation: `docs/investigations/2026-07-21-location-v2-auto-entries.md`. | Complete full validation and review; merge the implementation PR; deploy the merged API; set production `DAYFRAME_LOCATION_ROLLOUT_MODE=v2_enabled`; have the client acknowledge the mode; ship and verify the next internal TestFlight build; then run a new physical journey and record automatic-entry/Review results. Keep the checked-in fallback default at `v2_shadow`. |
| iOS place search, compact location settings, and scroll recovery | Watch | PR #87 merged as `e70155f` and shipped in TestFlight build `0.1.0 (60)`, delivery/build ID `eb75a47b-0cbc-4585-92b6-dfb14ab9e90f`; App Store Connect is `VALID` and the build is `IN_BETA_TESTING` through `Internal Health Debug`. Investigation: `docs/investigations/2026-07-21-mobile-place-search-settings-polish.md`. | Complete nearby ranking, POI, rapid typing, offline, permission/Precise Location, save/cancel/edit, learned-place, Light/Dark/System, Dynamic Type, diagonal-swipe, and blank-scroll recovery checks on a physical iPhone. |
| Native Calendar touch and data regression | Watch | PR #85 merged as `2c3e151` and shipped in TestFlight build `0.1.0 (58)`, delivery/build ID `35172e2f-16e4-438b-96fa-83d03691675a`; App Store Connect is `VALID`, export compliance is false, en-GB notes are set, and the build is `IN_BETA_TESTING` through the all-build `Internal Health Debug` group. Production logs confirmed repeated bootstrap/timer traffic. The retained SwiftUI view rendered its initial presentation but later nested Expo `Record` revisions were stale; the fix crosses Fabric as versioned JSON and adds executable initial/later active-entry decode coverage. Evidence: `docs/investigations/2026-07-19-native-calendar-touch-data-regression.md`. | On a physical iPhone, verify taps/swipes repaint the selected day/week; active/completed entries survive refresh and tab-away/tab-back; and vertical pan, pinch, entry taps, VoiceOver, Dynamic Type, Reduce Motion, and Reduce Transparency remain correct. |
| Tag entry refinement | Watch | PR #84 merged as `7b89fe5` and shipped in TestFlight build `0.1.0 (57)`, delivery/build ID `2d2687ce-2433-4ffb-91bd-352f05f0ea95`; App Store Connect `VALID`, export compliance false, en-GB notes set, and `IN_BETA_TESTING` in `Internal Health Debug`. It follows PR #83/build 56 with a contrasting theme-aware chooser, one solid rounded tag silhouette across mobile/web/native Calendar, and draft-only tap-to-remove behavior whose persistence remains owned by the checkmark save. | On a physical iPhone, verify chooser contrast in both themes and confirm tag removal persists on checkmark save but is discarded when the edit card is dismissed; also check VoiceOver, Dynamic Type, Reduce Motion, and compact Calendar blocks. |
| Reusable entry tags and inline hashtag autocomplete | Watch | PR #82 merged as `661e8c5` and shipped in TestFlight build `0.1.0 (55)`, delivery/build ID `31cde481-99bd-40df-bad9-0ab600ab76a4`; App Store Connect `VALID`, export compliance false, en-GB notes set, `Internal Health Debug` all-build access, and `IN_BETA_TESTING`. Hosted migration `202607180001_tags.sql` is applied; production columns, composite workspace constraints, RLS, and association policies were verified before release. The feature includes event-first transactional tag persistence, mobile/web caret autocomplete, Today/List/Calendar metadata, native Calendar presentation, web Tags management, and offline queue reconciliation. Broad validation passed with 432 tests plus Swift tests and a full iOS build. Motion/design/validation evidence: `docs/investigations/2026-07-18-tags.md`. | On authenticated web and a physical iPhone, verify create/rename/delete, caret and keyboard continuity, offline reconciliation, optimistic rollback, Calendar gesture ownership, VoiceOver, Dynamic Type, Reduce Motion, light/dark styling, and cross-workspace isolation. |
| Mobile Categories keyboard and palette distinction | Watch | PR #81 merged as `ce62372` and shipped in TestFlight build `0.1.0 (54)`, delivery/build ID `3513d3a6-81d2-466b-9d9c-6562291264b6`; App Store Connect `VALID`, export compliance false, en-GB notes set, `Internal Health Debug` all-build access, and `IN_BETA_TESTING`. It adds keyboard-adjusted focus reveal, the shared 12-colour picker to creation, and perceptual-distance/legacy-key guardrails. Focused and full workspace validation passed (396 tests), and authenticated simulator keyboard, light/dark, VoiceOver, Reduce Motion and Reduce Transparency checks passed. Investigation: `docs/investigations/2026-07-18-categories-keyboard-palette.md`. | Verify maximum Dynamic Type and the complete flow on a physical iPhone because the iOS 26 simulator text-size slider rejected Computer Use adjustment and no physical iPhone was connected during implementation. |
| Mobile Calendar refresh and Today group-action recovery | Watch | PR #79 merged as `a9d8254` and shipped in TestFlight build `0.1.0 (51)`, delivery/build ID `395ec984-05c9-4761-9d65-4e3cb59203bd`; App Store Connect `VALID`, export compliance false, en-GB notes set, `Internal Health Debug` all-build access, and `IN_BETA_TESTING`. Native Calendar now consumes refreshed `dayEntries`/`historyEntries`, history replay atomically switches the active timer, and collapsed-group deletion gained a five-second bottom Undo window before server persistence. Its original confirmation step was superseded by direct deletion in build `0.1.0 (52)`. No API, shared-package, Supabase, or web configuration change. | On a physical iPhone verify Friday/day selection and entries survive refresh/category-colour changes; replay switches exactly one timer; grouped deletion shows the correct count; Undo restores every child; expiry persists deletion; and VoiceOver, Dynamic Type, light/dark and Reduce Motion remain correct. |
| Report date-range filtering and configurable goals | Done | PR #78 merged as `d8d2954`. It applies one Day/Week/Month/Custom range across report totals, daily series and category/source/place breakdowns; clips cross-boundary entries; and replaces hard-coded 8h/40h goals with user-level `daily_goal_minutes`/`weekly_goal_minutes`. KB applied migration `202607170001_user_time_goals.sql` to hosted Supabase. Existing authenticated bootstrap/profile routes were extended without changing API base URLs, integration tokens, Cockpit configuration or RLS. | Verify the production Reports range controls and persisted Settings goals on desktop and phone widths. |
| Today history swipe-to-delete | Watch | PR #80 merged as `04aa708` and shipped in build `0.1.0 (53)`, delivery/build ID `3eb96b92-2a7e-4966-8c19-0b65ef6d36fa`; App Store Connect `VALID`, export compliance false, en-GB notes set, `Internal Health Debug` all-build access, and `IN_BETA_TESTING`. List and grouped deletions begin immediately without a confirmation modal, retain the five-second Undo path, and render the Undo notice as an inverse-colour bean with a coral action. The motion-consistency follow-up adds stable-key row/group presence and reflow, tokenized rapid-delete/Undo ownership, exact restoration, failure rollback, and consistent local motion across Today, Review, Places, date selection, and confirmations. No API, web-runtime, shared-package, Supabase migration, Vercel, or native Calendar ownership change was required. Investigations: `docs/investigations/2026-07-17-today-swipe-delete-motion.md` and `docs/investigations/2026-07-18-ios-motion-consistency.md`. | On a physical iPhone, verify direct individual/grouped/child/blank-row swipe deletion, vertical-scroll arbitration, Undo/expiry/rapid replacement/offline rollback, Review and Places transitions, light/dark inverse bean contrast, VoiceOver, Dynamic Type, Reduce Motion, and frame pacing before marking settled. |
| Native SwiftUI Calendar zoom correction | Watch | PR #72 merged as `3f9e97f`; no migration, web/API, or shared package change required; build `0.1.0 (47)`, delivery/build ID `7269d285-352c-41cf-84fd-e304aec94c6a`; App Store Connect `VALID`, export compliance false, en-GB notes set, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. SwiftUI now owns the 24-hour Calendar presentation and native scroll/pinch ownership while React Native still owns bootstrap data, mutations, routes, and editor/review sheets. Validation passed: `npm run test` (362 tests), Swift tests (9 tests), typecheck, lint, build, brand checks, `git diff --check`, TestFlight preflight, archive, export, upload, and ASC verification. A local generic simulator link check hit React Native debug arm64 symbol noise after the native module compiled; the device Release archive/export passed. Evidence and remaining risks are recorded in `docs/investigations/2026-07-16-native-calendar-zoom.md`. | On a physical iPhone, verify repeated pinch-in/out, vertical pan/deceleration, moving-midpoint anchoring, active/completed/review taps, day/week navigation, current-time line alignment, Reduce Motion, Reduce Transparency, VoiceOver, Dynamic Type, and frame pacing before marking settled. |
| Mobile history grouping and motion follow-up | Watch | PR #71 merged as `2c3173b`; no migration required; build `0.1.0 (46)`, delivery/build ID `7c372c50-15f7-40c2-a67c-582220c41da0`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel production verified the PR #71 runtime code as Ready at `https://dayframe-kb4uorezi-dayframeworkshop.vercel.app` for commit `2c3173b`; later docs-only release commits may advance the alias without changing runtime code. Investigation and closure evidence lives in `docs/investigations/2026-07-16-mobile-history-motion-followup.md`; its Calendar acceptance is superseded by the native Calendar item above. | On a physical iPhone, verify repeated Today entries collapse into useful grouped rows with editable children, Quick Actions sit naturally under the idle composer, description tap and Play both open the same running editor, and suggestions survive optimistic-to-persisted ID reconciliation. |
| Mobile timer and history UX follow-up | Watch | PR #70 merged as `660c0e9`; no migration required; build `0.1.0 (45)`, delivery/build ID `40ddc209-9735-4888-ad71-14c8edd448fd`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel production verified the PR #70 runtime code as Ready at `https://dayframe-l9rb2aueh-dayframeworkshop.vercel.app` for commit `660c0e9`; later docs-only release commits may advance the alias without changing runtime code. Design QA is recorded in `design-qa.md`. | On a physical iPhone, verify idle Today is a single immediate-start composer, running Today has one editable active-timer card, Add time is retrospective only, running-sheet suggestions stay visible through category/date changes and cap at six, the floating date picker works, the 60-day Today history and replay actions feel stable, and Live Activity/Shortcut category colours look correct. |
| Build 43 mobile regression recovery | Watch | PR #69 merged as `e1554e4`; no migration required; build `0.1.0 (44)`, delivery/build ID `c9c48192-4cc2-47a8-a272-0cf6bd6b5107`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel production verified the PR #69 runtime code as Ready at `https://dayframe-ogjqj5sq5-dayframeworkshop.vercel.app` for commit `e1554e4`; later docs-only release commits may advance the alias without changing runtime code. Investigation and closure evidence lives in `docs/investigations/2026-07-16-build43-mobile-regressions.md`; the newer no-visible-loading contract supersedes PR #67's timer-mutation progress treatment, and Calendar acceptance is superseded by the native Calendar item above. | On a physical iPhone, verify timer mutations remain visually immediate, active Play never creates a second timer, delete confirmation leaves sheet content fixed, saved-place monitoring rehydrates without false initial enters, and School is inside the monitored-region selection. |
| Mobile design language and timer flow polish | Watch | PR #68 merged as `7ec7a76`; no migration required; build `0.1.0 (43)`, delivery/build ID `510b1fd9-05b2-44f5-886c-f32c4b1a6a8b`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel was skipped as `Not affected` for this mobile/docs PR; the mobile archive uses API base `https://dayframe-web.vercel.app`. Calendar interaction acceptance is superseded by the native Calendar item above. | On a physical iPhone, verify empty Play starts immediately and opens `Edit timer`, suggestions update the existing running entry instead of starting a duplicate, the active card and sheet elapsed values stay synchronised, the capped suggestion list remains usable, and Today/date picker/Reports/Categories/Appearance follow the lighter fill-led iOS design without cropped controls or layout jitter. |
| Mobile sheet loading polish | Watch | PR #67 merged as `d020834`; no migration required; build `0.1.0 (42)`, delivery/build ID `576e1a4b-d38a-4167-be5b-359149b2f274`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel was skipped as `Not affected` for this mobile-only PR; the mobile archive uses API base `https://dayframe-web.vercel.app`. | Timer-mutation progress from this historical build is superseded by the Build 43 regression recovery: verify Edit Timer save is optimistic with no progress bar, spinner, or moving content. Continue checking the compact `Done` pill, swipe-down/outside-tap dismissal, keyboard-coupled motion, and learned-place sheet independently. |
| Instant-start adaptive suggestions | Watch | PR #66 merged as `2ee4892`; no migration required; build `0.1.0 (41)`, delivery/build ID `f080029e-d04f-4b7b-935b-f6782c37c46c`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-d8ihyb97h-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On a physical iPhone, verify empty Play starts immediately, opens the running edit sheet, keeps the active card and sheet elapsed values aligned, ranks suggestions by recency/frequency/time context, dismisses suggestions cleanly, and keeps pinned quick actions category-only. On web, verify the compact suggestions remain usable and auto/system history stays excluded. |
| Native iOS Liquid Glass tabs | Watch | PR #64 merged as `c240260`, followed by release redirect fix `abcb7b9`; no migration required; build `0.1.0 (39)`, delivery/build ID `6a3102c3-4fe5-45df-8c8c-77701a13cd55`; App Store Connect `VALID`, export compliance false, `Internal Health Debug` has all-build access, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-rgf41ngqg-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On a physical iPhone, verify native Liquid Glass tabs on Today/Calendar/Reports, scroll-down minimisation, safe content insets, dark/light appearance, Reduce Transparency, timer actions, Calendar gestures, and Settings push/back above the tabs. Also confirm the standard native tab appearance remains usable on the minimum supported iOS version. |
| Compact task suggestions refinement | Watch | PR #65 merged as `8b95f60`; no migration required; build `0.1.0 (40)`, delivery/build ID `b56f1311-d833-4d3a-8b2e-46c078fd348f`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-loqqpkwmv-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On a physical iPhone, verify empty Play starts one bare timer and opens its running edit sheet; the sheet shows the exact Active timer elapsed value plus up to 6 thin one-line suggestions above Description, hides them on outside/manual-entry interaction, and applies a row by patching the existing timer. On web, verify focus/click opens the compact autocomplete. Keep pinned quick actions pinned-only and automatic/unconfirmed history excluded. |
| Intelligent task entry suggestions | Watch | PR #62 merged as `c7d0f67`; no migration required; build `0.1.0 (37)`, delivery/build ID `80f37e6c-5c51-4aeb-ac5e-b2ce4ec6e4c4`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-bctakwm3p-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On a physical iPhone and web, verify pinned quick actions show pinned categories only, empty Play opens `SUGGESTIONS`, tapping a suggestion starts it immediately, `Start without details` preserves blank-timer use, sections are useful without being noisy, and auto/system entries such as sleep, workout walking, geofence visits, commutes, health imports, automation-created entries, and unresolved review items do not appear. |
| Web/mobile parity and place logging controls | Watch | PR #61 merged as `d53e79d`; migration `202607140002_place_logging_enabled.sql` applied by KB before release; build `0.1.0 (36)`, delivery/build ID `4f1e499c-e5dd-42aa-bacf-560246dbaf63`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Release Vercel deployment was Ready at `https://dayframe-4dq5o0aux-dayframeworkshop.vercel.app`. | On a physical iPhone and web, verify per-place logging can be disabled, logging-off places hide category/default description controls and do not create automatic review items, selected category pills use the softer fill with consistent ticks, web learned-place details support address/coordinate copy plus Save/Ignore/Forget, Review rows use clearer detected-visit/commute copy without accidental one-click accept, and the web calendar is fixed 24-hour. |
| Mobile UI consistency polish | Watch | PR #60 merged as `58e910f`; build `0.1.0 (35)`, delivery/build ID `9a6d7561-27cd-482c-bb85-fb94ad75ca47`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel alias `https://dayframe-web.vercel.app` remains Ready at `https://dayframe-b8fyfbutk-dayframeworkshop.vercel.app`; PR #60 is mobile-only, so web/API behaviour was not functionally changed by this release. | On a physical iPhone, verify Settings/Places/Review headers stay visible while scrolling, all Settings sub-settings retain usable back/title controls, button text is vertically centred, Learned Places `Save` is a rounded rectangle rather than an oval, selected category chips invert to white fill with category-coloured text/border/dot, play/stop inner glyphs are slightly larger without changing button size, and Calendar entry titles feel lighter. |
| Learned-place evidence classification and readable details | Watch | PR #59 merged as `b0904c0`; migration `202607140001_location_learning_intelligence.sql` applied by KB before release; build `0.1.0 (34)`, delivery/build ID `b06d7137-2205-4120-9840-644e2ba51bcb`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-8872s0wlu-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On a physical iPhone, verify weak one-visit clusters stay hidden, a single 60+ minute stable stay appears only in Review, repeat visits can become a Place suggestion, legacy coordinate names resolve, and address copy pastes cleanly into Maps. |
| Location trust and commute candidate correctness | Watch | PR #58 merged as `46e4217`; build `0.1.0 (33)`, delivery/build ID `c0304b80-293c-42aa-9873-79ccc05acb8b`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-mpbgzyaig-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On device, verify Home GPS drift within roughly 20m does not create an unknown learned place, one-off visits do not surface as learned places, clean saved-place commutes auto-log category-only `Commute`, uncertain commutes appear as dotted Calendar review candidates, ignored learned places do not return, and reports count confirmed entries only. |
| Today/Review/Places location polish | Watch | PR #57 merged as `8095bde`; build `0.1.0 (32)`, delivery/build ID `c6298983-04bb-439c-b36f-14e2c09a9d8a`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-h22k3bwge-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. | On a physical iPhone, verify hatched `Uncategorized`, tappable Today Review notice, readable learned-place names, learned-place detail actions, detected-visit Review copy, `Places & Location` settings rename, and Shortcuts/NFC panel removal. |
| Intelligent commute tracking and regular-place learning | Watch | PR #56, build `0.1.0 (31)`, delivery/build ID `6a66f8de-e4c8-4a44-ba77-1fb76d48148b`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production is Ready at `https://dayframe-nba09ddcn-dayframeworkshop.vercel.app` and aliased to `https://dayframe-web.vercel.app`. Production Supabase migration `202607120002_location_learning.sql` was applied and `public.learned_places` exists with RLS policies. | Verify the location toggle remains off by default, learned candidates appear under Places with Save/Ignore capped at 10, saved learned places disappear from the learned list, commute candidates stay Review-first, and confirmed commutes log as category-only `Commute` entries that show in weekly reports. |
| Today timer improvements verification | Watch | PR #54, build `0.1.0 (29)`, delivery/build ID `0ee6fe8f-13b3-40fd-aa7f-c692333db0d1`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production deployment for merge `bda205b` succeeded at `https://dayframe-93n6cwfd7-dayframeworkshop.vercel.app`. | Verify the Start Task sheet shows `--:--` until play, replacing an active timer stops the old timer and starts one new timer without the `Timer not started` error, no-description/no-category starts still work, Quick Actions still replace the active timer, grey `Elapsed`/`Running` labels are gone, Start Task/Edit Timer respect the Dynamic Island and keyboard safe area, the description field stays visible while typing, and Edit Timer no longer has excessive blank space below `Delete entry`. |
| Generic integration timer API and natural-language rule creation verification | Watch | PR #50, build `0.1.0 (25)`, delivery/build ID `605005f7-f4c3-4f19-89cb-48ed87b0829a`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. Vercel production deployment for `c24be99` completed, production migration `202607120001_automation_rule_activity_description.sql` was applied, and `automation_rules.activity_description` exists. | Verify Cockpit/API current timer read, saved natural-language rule creation, Review-first geofence rule output, and the Live Activity expanded-label nudge on device/hosted production. |
| Live Activity Dynamic Island polish verification | Watch | PR #49, build `0.1.0 (24)`, delivery/build ID `30ad5c74-704f-4d64-80a7-c721467df1cb`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. | Verify compact Dynamic Island width, expanded Live Activity text alignment, and the Live Activity stop button on a physical iPhone. |
| PR48 real-device Shortcut/NFC + Live Activity verification | Watch | PR #48, build `0.1.0 (23)`, delivery/build ID `8ecf06ec-7953-489e-85cf-5c21fcf3e15e`; App Store Connect `VALID`, export compliance false, `Internal Health Debug`, and `IN_BETA_TESTING`. | On a physical iPhone, test Shortcut/NFC Start and Stop while Dayframe is closed/suspended, check offline queue replay, and confirm the Live Activity/Dynamic Island timer is the visible confirmation surface instead of a Dayframe foreground splash. |

## Web App Parity Backlog

| Item | Status | Evidence | Next action |
| --- | --- | --- | --- |
| Web Today summary and Uncategorized treatment parity | Done | PR #77 merged as `0f910c7`; neutral `Uncategorized` treatment now covers web summaries, charts, legends and related rows, Review surfaces share the open-item filter and explicit navigation, and active-timer start time uses `HH:MM` masking and validation. No API, database, mobile or Supabase migration change was required. | Continue production observation at desktop and phone widths. |
| Web Review detected-visit clarity | Watch | PR #61 updated web Review wording for detected visits/commute suggestions and removed dashboard-row one-click accept; build `0.1.0 (36)` and Vercel production contain the change. | Verify on web with real review items that the copy explains what Dayframe detected, what accepting will create, and does not imply certainty from location evidence alone. |
| Web learned-place detail management | Watch | PR #61 added richer web learned-place detail management with address/coordinate copy and Save/Ignore/Forget; build `0.1.0 (36)` and Vercel production contain the change. | Verify on web that learned-place rows open details, copy actions work, Save/Ignore/Forget semantics are clear, and saved/ignored candidates leave the list correctly. |
| Shared readable learned-place naming for web and mobile | Watch | PR #57 added shared readable location-name resolution and server-side normalization for learned-place/review payloads; build `0.1.0 (32)` contains the mobile side and Vercel production contains the server/shared side. | Verify newly queued learned places and detected visits prefer venue/address/locality/postcode over coordinates in both mobile and web surfaces. Watch geocoding frequency because background reverse geocoding now runs only when a learned-place candidate is queued. |
| Web calendar 24-hour and cross-midnight parity | Watch | PR #61 removed the web Awake/24h split and aligned the web calendar with the fixed 24-hour mobile model; build `0.1.0 (36)` and Vercel production contain the change. | Verify sleep, commute, and cross-midnight blocks remain visible with clear 00:00 boundary/continuation behaviour in web calendar and weekly reporting. |

## Recently Shipped Or Addressed

| Item | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Mobile history grouping and motion follow-up | Watch | PR #71, build `0.1.0 (46)`, delivery/build ID `7c372c50-15f7-40c2-a67c-582220c41da0`; no migration required. Vercel production verified PR #71 runtime commit `2c3173b` as Ready; later docs-only release commits may advance the alias without changing runtime code. | Groups repeated Today history by normalized category/task, adds collapsed count rows and editable expanded children, moves Quick Actions under the idle composer, stabilises running suggestions across optimistic timer reconciliation, and shifts Calendar pinch work off per-frame React state. The physical-iPhone Calendar result did not meet product acceptance and is now superseded by the native SwiftUI Calendar `Watch` item. |
| Mobile timer and history UX follow-up | Watch | PR #70, build `0.1.0 (45)`, delivery/build ID `40ddc209-9735-4888-ad71-14c8edd448fd`; no migration required. Vercel production verified PR #70 runtime commit `660c0e9` as Ready; later docs-only release commits may advance the alias without changing runtime code. | Refines Today into one idle composer or one active-timer card, adds retrospective Add time, keeps running-sheet suggestions in the edit flow, adds the floating date picker and 60-day history/replay actions, carries category colour into Live Activity/Shortcuts, and removes the Settings intro card. |
| Build 43 mobile regression recovery | Watch | PR #69, build `0.1.0 (44)`, delivery/build ID `c9c48192-4cc2-47a8-a272-0cf6bd6b5107`; no migration required. Vercel production verified PR #69 runtime commit `e1554e4` as Ready; later docs-only release commits may advance the alias without changing runtime code. | Fixes the build 43 regressions across invisible optimistic timer mutations, active Play/running-timer suggestions, stable delete confirmation, score-led task suggestions, geofence rehydration/diagnostics, and anchored Calendar zoom. |
| Mobile design language and timer flow polish | Watch | PR #68, build `0.1.0 (43)`, delivery/build ID `510b1fd9-05b2-44f5-886c-f32c4b1a6a8b`; no migration required. | Moves mobile suggestions into the running Edit Timer sheet capped at six, keeps empty Play immediate, applies suggestions to the already-running entry, preserves one elapsed-time source for card and sheet, refreshes the iOS visual language with fill-led surfaces/circular actions/pill controls/lighter hierarchy, adds the floating Today date picker, updates Today/Calendar/Reports/Categories/Appearance, fixes cross-midnight Reports category overlap accounting, and records side-by-side design QA. |
| Mobile sheet loading polish | Watch | PR #67, build `0.1.0 (42)`, delivery/build ID `576e1a4b-d38a-4167-be5b-359149b2f274`; no migration required. | Shipped the slim `Done` pill, compact controls, suggestion rows, and keyboard-coupled sheet motion. Its sheet-edge progress treatment for timer mutations is historical and superseded by the current no-visible-loading contract; learned-place mutation progress is outside that timer-specific rule. |
| Instant-start adaptive suggestions | Watch | PR #66, build `0.1.0 (41)`, delivery/build ID `f080029e-d04f-4b7b-935b-f6782c37c46c`; no migration required. | Speeds up empty mobile Play by creating the bare running timer optimistically, opens the running edit sheet, ranks suggestions by frequency/recency/time context, uses category usage for pinned category ordering, smooths suggestion dismissal, tightens edit-sheet spacing, and removes the active-card delete button. |
| Compact task suggestions refinement | Watch | PR #63 shipped the first pass in build `0.1.0 (38)`; PR #65 corrected the running-timer suggestion flow in build `0.1.0 (40)`; no migration required. | Mobile empty Play starts a bare timer and transitions into Edit timer, where up to 6 divider rows enrich that same running entry. The Active card and sheet share the exact elapsed calculation. Web suggestions remain a focus/click autocomplete. The shared ranking keeps newest eligible manual work first, then frequent/contextual manual tasks. |
| Intelligent task entry suggestions | Watch | PR #62, build `0.1.0 (37)`, delivery/build ID `80f37e6c-5c51-4aeb-ac5e-b2ce4ec6e4c4`; no migration required. | Restores pinned quick actions to pinned categories only, moves learned/manual activity into `SUGGESTIONS` on empty Play, keeps `Start without details` for blank timers, starts selected suggestions immediately, and shares a mobile/web scoring model that trains only from manual or confirmed entries while excluding auto/system activity by provenance. |
| Web/mobile parity and place logging controls | Watch | PR #61, build `0.1.0 (36)`, delivery/build ID `4f1e499c-e5dd-42aa-bacf-560246dbaf63`; production migration `202607140002_place_logging_enabled.sql` applied by KB before release. | Adds per-place logging controls across mobile and web, prevents logging-disabled places from creating automatic visit review items, gives web learned-place details address/coordinate copy plus Save/Ignore/Forget, clarifies web Review copy and dashboard-row behaviour, aligns the web calendar to fixed 24-hour mode, and softens selected category pills with consistent ticks. |
| Mobile UI consistency polish | Watch | PR #60, build `0.1.0 (35)`, delivery/build ID `9a6d7561-27cd-482c-bb85-fb94ad75ca47`. | Keeps Settings/Places/Review back/title headers fixed above scrolling content, tightens shared mobile button centering and line heights, changes Learned Places `Save` from oval to rounded rectangle, makes category chips thinner with selected colour inversion, slightly enlarges play/stop glyphs without resizing buttons, and reduces Calendar entry title weight. |
| Learned-place evidence classification and readable details | Watch | PR #59, build `0.1.0 (34)`, delivery/build ID `b06d7137-2205-4120-9840-644e2ba51bcb`; production migration `202607140001_location_learning_intelligence.sql` applied by KB before release. | Server-enforced classification hides weak/pass-through clusters, routes long single stays to one-off Review items, requires repeat evidence before saveable Places, keeps learned-place evidence monotonic, caches/backfills readable POI/address data, and makes address/coordinates copyable from the mobile detail sheet. |
| Location trust and commute candidate correctness | Watch | PR #58, build `0.1.0 (33)`, delivery/build ID `c0304b80-293c-42aa-9873-79ccc05acb8b`. | Adds accuracy-aware saved-place snapping for GPS drift, repeat-visit/distinct-day thresholds before learned places surface, ignored learned-place cluster suppression, saved-place-to-saved-place category-only commute auto-log, uncertain commute Review/calendar previews, and Calendar filtering so learned-place visits do not appear as activity blocks. |
| Today/Review/Places location polish | Watch | PR #57, build `0.1.0 (32)`, delivery/build ID `c6298983-04bb-439c-b36f-14e2c09a9d8a`. | Gives `Uncategorized` a neutral hatched treatment, makes the Today review notice tappable, resolves learned/detected-place names through venue/address/locality/postcode before coordinates, adds learned-place details with Save/Edit/Ignore/Forget, clarifies detected-visit Review copy, renames settings to `Places & Location`, and removes the Shortcuts/NFC panel from that screen. |
| Today timer setup polish | Watch | PR #51 covered web only; PR #52, build `0.1.0 (27)`, corrected the first iOS Today timer polish pass; PR #53, build `0.1.0 (28)`, shipped the setup-sheet and Live Activity fixes; PR #54, build `0.1.0 (29)`, delivery/build ID `0ee6fe8f-13b3-40fd-aa7f-c692333db0d1`, fixes the Start Task/Edit Timer sheet replacement and keyboard-safe layout regressions. | Removes the old visible `Running` fallback/status from the iOS Today active timer, improves blank active timer copy to `Add a task description`, keeps `Uncategorized` visible under uncategorized running tasks while removing it from quick actions, renames `Frequent` to `Quick actions`, makes quick-action/pinned category pills visually thinner while preserving tap comfort, smooths Active Timer panel expansion/collapse, opens a Start Task sheet from the description field, keeps Edit Timer save neutral vs destructive Stop, adds Edit Timer `Delete entry`, restores the compact/minimal Dynamic Island timer icon, shows `--:--` until a new sheet-started timer is created, and safely replaces any active timer in one server transaction. |
| Commute and regular-place learning | Watch | PR #55 introduced location learning in build `0.1.0 (30)`; PR #56, build `0.1.0 (31)`, delivery/build ID `6a66f8de-e4c8-4a44-ba77-1fb76d48148b`, adds learned-place management and category-only commute logging; PR #57/build `0.1.0 (32)` improves readable learned-place/review names. | Adds opt-in Settings > Places & Location learning, coarse sample capture, review-first commute candidates between saved-place visits, learned unsaved-place rollups in `learned_places`, Places > Learned places Save/Ignore actions capped at 10 candidates, and confirmed commute entries under the `Commute` category without a default description. |
| Dayframe/Cockpit live timer integration | Watch | PR #50 shipped the generic `GET /api/integrations/v1/time/current` endpoint with `time:read`; Desk Cockpit `/api/dayframe` now returns `configured:true` against the production API. | Current read-only timer strip is live and should be checked on the Cockpit iPad before settling the card. Broader today timeline, source/confidence, next suggested action, and token-management UI remain future work. |
| Generic integration timer API and natural-language rule creation | Watch | PR #50, build `0.1.0 (25)`, delivery/build ID `605005f7-f4c3-4f19-89cb-48ed87b0829a`; production migration `202607120001_automation_rule_activity_description.sql` applied. | Adds the generic `GET /api/integrations/v1/time/current` endpoint with `time:read` tokens, documents the integration API, lets the automation assistant save review-first geofence-exit rules with category/project/activity description, hardens workspace scoping, and bundles the Live Activity expanded-label nudge. |
| Live Activity Dynamic Island polish | Watch | PR #49, build `0.1.0 (24)`, delivery/build ID `30ad5c74-704f-4d64-80a7-c721467df1cb`. | Tightens compact Dynamic Island content to a small timer plus stop control, reworks expanded Live Activity text/control alignment to avoid clipping, and adds an iOS 17 Live Activity stop button backed by the existing native stop queue. |
| Background Shortcuts and Live Activity | Watch | PR #48, build `0.1.0 (23)`, delivery/build ID `8ecf06ec-7953-489e-85cf-5c21fcf3e15e`. | Adds native queued Start/Stop App Intent handling for Shortcut/NFC triggers, drains native Shortcut events through the normal mobile offline queue, adds a Live Activity/Dynamic Island timer confirmation surface, and bundles the Settings > Automations > Places cleanup that removes the distracting right-side logo/spinner. Watch real-device Shortcut/NFC foreground behaviour, suspended/offline queue replay, and Live Activity/Dynamic Island lifecycle before marking settled. |
| Mobile navigation continuity and dark transition backgrounds | Watch | PR #47, build `0.1.0 (22)`, delivery/build ID `a2fa4831-5c22-4f2b-b455-f4263c2ee03c`. | Binds Expo Router/native containers to Dayframe light/dark theme surfaces, uses flat gesture-compatible native-stack transitions without the rounded-card shadow, centralises Reduce Motion-aware timings, removes the delayed description-field scroll jump, and includes the `ExpoSystemUI` native pod for runtime root background updates. Verify dark/light Settings, Review and Places push/pop/swipe-back transitions on a physical iPhone, including Reduce Motion. |
| Settings cached navigation and silent refresh | Watch | PR #46, build `0.1.0 (21)`, delivery UUID `56aafea3-fe4a-491e-a57a-ac3a7840be14`. | Settings navigation renders from cached data immediately, silently refreshes stale snapshots in the background, only shows the pull-to-refresh spinner when the user explicitly pulls, and avoids Health/location diagnostic work on unrelated sub-settings. Watch real-device Settings navigation across Profile, Categories, Appearance, Automations, Health, and Sync before marking settled. |
| Settings sub-settings back navigation | Watch | PR #45, build `0.1.0 (20)`, delivery UUID `6bacb90a-006e-49f6-acc4-e1d2fbe64ee2`. | All Settings sub-settings now derive from the `section` route param only. Removed local `settingsSection` mutation and the manual `beforeRemove` interception so native iOS swipe-back and the header back button can pop normally to the Settings index. Watch real-device navigation through every Settings sub-section before marking settled. |
| Calendar compact blocks and Settings swipe-back | Watch | PR #44, build `0.1.0 (19)`, delivery UUID `997c7e8e-9556-4a54-a586-c71e1b80061b`; PR #45, build `0.1.0 (20)`, followed up the remaining Settings sub-settings back-navigation glitch. | Calendar blocks now keep near-true duration height and suppress title/meta labels until there is enough vertical space. PR #44 did not fully fix Settings sub-setting back navigation because the old Settings route could retain local sub-setting state; PR #45 handles that follow-up. |
| Mobile review polish and Shortcut stop follow-up | Watch | PR #43, build `0.1.0 (18)`, delivery UUID `5540f30c-5002-4a0b-a217-709eed42c6a7`; PR #44, build `0.1.0 (19)`, followed up the failed swipe-back fix. | Settings header logo removed; timer start/stop uses a subtle progress bar instead of a layout-jumping full spinner; saved-place review reasons explain review-first visits; Shortcuts adds `Stop tracking` plus cached category/workspace options for `Start tracking`. Shortcut starts/stops still show the deep-link queued splash, so that is split into the background Shortcuts/Live Activity follow-up. |
| Siri Shortcuts, NFC handoff, and mobile Settings grouping | Watch | PR #42, build `0.1.0 (17)`, delivery UUID `81a9567e-18db-4d0a-9eda-34e24aa71882`. | Adds the first iOS `Start tracking` App Intent for Apple Shortcuts/NFC automations with description and category parameters, removes Shortcut/NFC auto-default mappings, uses Dayframe's current workspace, preserves the offline queue handoff, and reorganizes mobile Settings into grouped sub-settings. Watch real-device Shortcut/NFC setup and category matching before marking fully settled. |
| More reliable offline and mobile sync | Watch | PR #41, build `0.1.0 (16)`, delivery UUID `c84e56c7-931a-4b4b-b28c-b6a66be04219`. | Adds retry backoff metadata, automatic foreground/focus queue drain, manual force sync/retry, queue diagnostics counts, and diagnostics export for mobile queued events. Watch real-device Shortcut/NFC/geofence/Health queue recovery, idempotency, and reconnect behaviour. |
| Auto-log defaults during onboarding and non-Health imports | Watch | PR #40, build `0.1.0 (15)`, delivery UUID `e6bcc257-2214-49c9-8614-5a201958a175`; PR #42, build `0.1.0 (17)`, corrected the Shortcut/NFC direction. | Auto-log defaults are only for user-enabled automatic sources such as Health, walking/running, places, and future commute/place learning. Manual mobile starts and Siri Shortcut/NFC starts use only the values supplied by the user or Shortcut. |
| Duplicate/overlapping Sleep investigation | Watch | `docs/investigations/2026-07-11-duplicate-sleep.md`; KB reports no current duplicates in TestFlight build `0.1.0 (14)`. | Existing idempotency, Health segment dedupe, covering-entry checks, overlap blocks, and legacy consolidation already guard the known paths. Do not add merge/delete logic without a real duplicate export or row sample. |
| Midnight Core reskin and supplied branding | Done | PR #39, build `0.1.0 (14)`, delivery UUID `e6425673-8e83-4d62-ae31-cc01e7fc6001`. | Shared web/iOS Midnight Core tokens, refreshed app icon, wordmarks, reusable brand components, and automated brand/theme guardrails shipped without changing core tracking logic. |
| Calendar edit card keyboard avoidance regression | Watch | PR #37, build `0.1.0 (13)`, delivery UUID `8b5d4ac4-d0ca-4239-9719-4442aee56ec6`. | Edit sheet now uses screen-coordinate keyboard measurements, explicit keyboard-open sheet height, scrollable form body, and regression tests for small-iPhone keyboard-open layout. Watch KB's real-device keyboard/suggestion-bar check before marking fully settled. |
| Cross-midnight continuation border polish | Done | PR #36, build `0.1.0 (12)`. | Continuation segments that started before midnight now drop the top border/top radii; segments continuing into the next day still drop the bottom border/bottom radii. |
| Apple Health auto-log category and description customization | Watch | PR #36, build `0.1.0 (12)`. | Settings can map HealthKit sleep/workout types to category and description defaults; new imports and Health Review reprocess both use the mappings. Watch real device import/reprocess behaviour before marking fully settled. |
| Calendar event tap crash | Done | PR #35, build `0.1.0 (11)`. | Root cause was unstable React hook order in `ActiveTimerEditSheet`. |
| HealthKit automatic sleep/workout sync | Watch | PR #35, build `0.1.0 (11)`. | Foreground sync and HealthKit observer callbacks are wired; keep watching real device/background behaviour. |
| Anchored pinch zoom | Done | PR #35, build `0.1.0 (11)`. | Zoom now shifts scroll position around the finger midpoint. |
| Fixed 24-hour calendar mode | Done | PR #35, build `0.1.0 (11)`. | Awake/24h chips and labels are gone; Calendar is fixed 24-hour. |
| Current-time red-line label removal | Done | PR #35, build `0.1.0 (11)`. | Red line no longer prints the current time over left-side timestamps. |
| Calendar swipe reliability and midnight labels | Done | PR #34, build `0.1.0 (10)`. | Looser diagonal day swipes, top day-strip week swipes, and 00:00 boundary labels. |
| Previous-day sleep continuation marker removal | Done | PR #34, build `0.1.0 (10)`. | Previous-day sleep continuation copy/styling was removed; next-day continuation styling still needs the top-border polish above. |
| Calendar 24h option, cross-midnight clipping, and reports polish | Done | PR #33, build `0.1.0 (9)`. | Added 24h calendar support, continuation visuals, pie/bar Reports toggle, and edit-sheet polish. |
| Edit-sheet keyboard, safe-area, and swipe-down polish | Watch | PR #33, build `0.1.0 (9)`. | Generic edit-sheet polish shipped, but the Calendar edit card now has a keyboard-covering regression tracked in Current Next Work. |
| Sleep category routing for new/reprocessed HealthKit sleep | Done | PR #32 and PR #33. | Sleep now uses/creates a user-facing `Sleep` category; workouts remain under `Health`. |
| Legacy Health-category Sleep repair | Done | PR #33. | Narrow cleanup path for old confirmed HealthKit sleep rows that were still under `Health`. |
| Mobile calendar gestures, pinch density, and delete entry action | Done | PR #32. | Added day swipe, pinch zoom, and completed-entry delete affordance. |
| Rule assistant draft/simulate slice | Done | PR #30. | Plain-language automation requests can draft evidence checks, simulation checks, outcomes, and unsupported gaps. |
| Incomplete review durations showing huge live-to-now values | Done | PR #30. | Incomplete suggestions no longer accrue absurd durations such as hundreds of hours. |
| Health Review backlog drain | Watch | PRs #25, #26, #27, and #29. | Bounded reprocess batches, prioritisation, covered-row handling, and legacy cleanup migration are merged; watch live backlog behaviour. |
| Stale reviewed cards / already-resolved popups | Watch | PRs #22, #26, and #30. | Confirm/Dismiss became idempotent or structured; mobile removes resolved cards optimistically. |
| Sleep stages fragmented into REM/Core/Deep cards | Watch | PRs #23, #26, and #29. | Current imports group sessions; legacy sleep-stage backlog cleanup is merged. |
| Health items left in Review without useful explanation | Watch | PRs #22, #25, #26, #27, and #29. | Diagnostics and left-in-review reasons exist; continue checking that reasons are clear on device. |
| Geo/place default description mismatch | Done | PR #19 and PR #26. | Place names should remain reference/context; configured activity descriptions are used for geofence display and Confirm. |
| TestFlight release preflight and compliance checks | Done | PR #31 plus build release runs through `0.1.0 (51)`. | Keep verifying processing state, encryption compliance, notes, and internal testing assignment before asking KB to test. Docs-only PRs do not need a TestFlight build unless they change release/build configuration. Build 31 fixed the app `Info.plist` to use `$(CURRENT_PROJECT_VERSION)` so the app and Live Activity extension build numbers match during archive/export, and builds 32-51 verified that path again. |

## Future Tracks

| Item | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Native NFC scanning beyond Shortcuts | Future | PR #42 shipped the preferred Apple Shortcuts-driven NFC route; NFC tags trigger Shortcuts, not Dayframe directly. | Revisit native NFC scanning only if Apple Shortcuts is not enough. Any native path should still queue idempotent start/stop/review events and keep user-controlled setup. |
| Expanded Dayframe integration with Cockpit | Future | The read-only current-timer slice is in Watch via PR #50 and the deployed Cockpit timer strip. | Extend beyond the current timer only after KB verifies the strip: today timeline, source/confidence display, next suggested action, token-management UI, and no write/mutation controls unless explicitly approved. |
| Natural-language rules that create time entries | Planned | PR #50 builds on the PR #30 draft/simulate slice with deterministic, review-first rule saves; no active PR is open. | Keep sequence/calendar auto-write execution out of v1 until simulation and audit controls are stronger; first saved version creates review items that become time entries only after confirmation. |
| Dayframe Preview / pre-prod lane | Future | Repeatedly noted as useful after the current fast TestFlight lane stabilises. | Create a separate preview/staging path with its own app identity or bundle, staging Vercel/Supabase environment, staging secrets, TestFlight group/build lane, and release checklist so KB can test risky changes before production/main TestFlight. |
| Durable repo tracker | Done | This document. | Update this file whenever a planned item moves, ships, is skipped, or needs live-watch status. |
| Full automation rule creation/editing | Future | PR #30 proves draft/simulate only. | Needs UI for saving, simulating, explaining, editing, disabling, and auditing rules. |
| Account deletion, workspace deletion, and stronger privacy controls | Future | README privacy model calls export/deletion next-phase work and raw event payloads are stored in `activity_events.raw_payload`. | Add user-facing export/delete flows for accounts and workspaces, hard-delete or anonymise scoped data, clear raw Health/location payloads and integration tokens, respect retention windows, and document what remains in backups/logs. |
| Geofence and place automation expansion | Future | Earlier Dayframe planning covered HA/iOS geofence triggers for Gym, Home, School, Church, Office/client sites, family places, town/Chelmsford centre, and unknown stays. README and production-readiness docs already have event-first geofence foundations. | Expand location automation carefully: improve place setup/correction, use review-first defaults for ambiguous/broad/Home locations, support specific enter/exit rules, avoid automatic writes without confidence, and keep source evidence visible for every suggestion or created entry. |
| Home Assistant/local bridge inputs | Future | Production-readiness docs describe scoped ingest tokens and a future Home Assistant bridge payload; prior planning included HA buttons/zones as local signals. Trello also tracks Apple Home smart buttons here because Home automations run from the home hub rather than a normal iPhone Shortcut surface. | Add token-management UI first, then a small local bridge path for HA button/geofence/Apple Home smart-button events into `/api/events`, with scoped/revocable tokens, idempotent payloads, and no uncontrolled direct time-entry writes. |
| Telegram/voice diary and correction intake | Future | Original life-tracking planning included Telegram/direct-chat corrections and end-of-day diary summaries that Major can turn into time entries. | Design a review-first correction/import lane where chat or voice notes become proposed edits or missing entries, show the evidence text, require confirmation before writing, and preserve an audit trail of what changed. |
| Review split/merge and saved-place correction flows | In progress | Location Intelligence V2 implements transactional edit/confirm, split, merge, record-once, saved-place correction, and save-place-and-confirm with evidence lineage and audit metadata on `codex/location-intelligence-v2`. Disposable-database tests passed rollback, retry/idempotency, concurrent confirmation, split lineage, compatible merge, and incompatible-merge rejection. | Complete independent browser/staging interaction, mobile/web accessibility parity, and physical-device review before treating the flow as shipped. |
| Reporting, export, backup, and restore confidence | Future | README and production-readiness docs expose workspace/time-entry/activity/review exports, but restore and larger-data reporting confidence are still future work. | Make Dayframe dependable as a system of record: richer filters, larger-data performance checks, CSV/JSON exports KB can trust, backup verification, restore/import tooling, and clear recovery docs. |

## Maintenance Rules

- Update status immediately after a PR merge, TestFlight build, or KB validation report.
- Keep `Watch` for fixes that are merged but depend on real HealthKit, geofence, or production data behaviour.
- Add PR/build evidence when moving an item to `Done`.
- Do not treat a chat plan as done unless GitHub/local code or TestFlight state verifies it.
